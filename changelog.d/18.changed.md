**Breaking:** `POST /threads/{id}/runs` is now a background create that returns immediately with a non-terminal `RunModel`, instead of blocking until the graph finished. To run synchronously and get the output back, call the new `POST /threads/{id}/runs/wait` (which returns the final graph state values and the `X-Tokens-Used` header).
